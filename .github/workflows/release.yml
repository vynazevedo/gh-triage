name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    name: Compila binários e publica o release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Compila e anexa binários (linux, macOS, windows)
        uses: cli/gh-extension-precompile@v2
        with:
          go_version_file: go.mod
